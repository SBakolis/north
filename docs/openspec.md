# OpenSpec context

When a project uses OpenSpec, agents read its project instructions and relevant
requirements, design, and task artifacts directly. Include the applicable paths
and constraints when delegating to subagents.

North does not install OpenSpec, normalize its artifacts, run an adapter, or
update change status automatically. Follow the project's OpenSpec workflow and
keep modifications within the user's requested scope.
