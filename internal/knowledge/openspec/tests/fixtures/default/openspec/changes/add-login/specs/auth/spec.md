## ADDED Requirements

### Requirement: Login
The service SHALL authenticate a valid user.

#### Scenario: Valid credentials
- **WHEN** valid credentials are submitted
- **THEN** an authenticated session is returned
